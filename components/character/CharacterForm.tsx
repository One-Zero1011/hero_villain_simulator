// This component has been replaced by AddCharacterModal.tsx
// Keeping empty file or safe placeholder to avoid build errors if lazy loaded, 
// though direct imports should be removed from parent.
import React from 'react';
const CharacterForm = () => null;
export default CharacterForm;